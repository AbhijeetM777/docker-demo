apiVersion: apps/v1 
kind: Deployment 

metadata:
 name: docker-demo

spec: 
 replicas: 2 

selector: 
 matchLabels:
  app: docker-demo

template: 
 metadata: 
  labels: 
   app: docker-demo

spec: 
 containers: 
 - name: docker-demo 
   image: abhijeetam7/docker-demo:v1 
   imagePullPolicy: Always


   ports:
   - containerPort: 80
